# app.oshihitsuji.jp — このリポジトリの決まり

ブラウザだけで動く小さなアプリを、横に並べて置いているリポジトリです。
いまは `log/`（記録アプリ「ログ」）と `wake/`（タスク管理「ワケワケ」）の2つ。

**新しいアプリを作る・増やすときは、`.claude/skills/pwa-app-build/SKILL.md` の
手順を通してください。** ここに書くのは、どの作業でも常に守る決まりだけです。

## 並びの決まり

**1つのフォルダが、1つのアプリで、1つの公開先。**
リポジトリの並びが、そのまま公開されるサイトの並びになります。

- **ドメイン直下（`/`）には何も置きません。** アプリは必ずフォルダの中に入れます
- 公開手順は「`index.html` を持つフォルダをそのまま並べる」と書いてあるだけです。
  アプリを増やすときに `.github/workflows/pages.yml` を編集する必要はありません
- アプリの中の参照はすべて相対パスにします。絶対URLを書いてよいのは
  `og:url` / `og:image` と manifest の `id` だけです

この形は、置き場所を変えたときにインストール済みアプリが壊れた経験から決めたものです。
崩すときは、その影響（下記「落とし穴」）を確認してからにしてください。

## コードの作法

既存2アプリはどちらも同じ作りです。新しいアプリもそろえてください。

- **ビルドしません。** バニラJS、外部ライブラリなし、`<script>` を並べるだけ
- モジュールは IIFE（`var Store = (function () { 'use strict'; ... })();`）
- `var` と `function`。ファイルの冒頭に、罫線で囲った日本語の説明ブロックを置く
- コメントは日本語で、「何を」ではなく **「なぜそうしたか」** を書く
- **Webフォントを使いません**（オフラインで見え方が変わらないように）
- ライト／ダーク自動対応。`color-scheme: light dark` を入れて、日付ピッカーなど
  ブラウザ標準の部品も明暗が合うようにする
- スマホでの片手操作が前提。下タブ、下から出るシート、`env(safe-area-inset-*)`

## データの決まり

- 保存は **localStorage のみ**。サーバーには何も送りません
- **キーはアプリごとに分けます**（`actionlog.v1` / `wake.v1`）。他のアプリの
  データには絶対に触らないこと
- 保存データに `version` を持たせ、`migrate` で世代移行する。
  **移行では既存の記録を消さず、足りないものを足すだけ**にする
- 消える前提のものなので、JSON の書き出し／復元を必ず用意する

## 変更したら、ブラウザで確かめる

コードを直したら、読み返すだけで終わらせず、実際に動かして確認してください。
手順は skill に書いてあります。最低限、次の3つは見ること。

1. スマホサイズで実際に操作して、コンソールエラーが出ないこと
2. 公開手順を実際に流して、出来上がる並びが意図どおりであること
3. `sw.js` のキャッシュ対象ファイルが全部実在すること
   （1つでも欠けると `addAll` が失敗し、オフラインが丸ごと無効になる）

## 落とし穴

一度実際に踏んだものです。くわしくは skill を参照。

- **manifest の `id` を省かない。** 省くと `start_url` が同一性の代わりになり、
  アプリの置き場所を変えた瞬間、インストール済みのものが別アプリ扱いになって開けなくなる
- **Service Worker はアプリのフォルダの中に置く。** 上の階層に置くとスコープが
  下位を全部拾い、隣のアプリのリクエストにまで口を出す
- **ドメイン直下に Service Worker を置かない。** 一度登録されると、ファイルを消しても
  更新に失敗して端末に居座り続ける

## その他

- `handoffs/` はブログ運営プロジェクトの引き継ぎ資料です。アプリとは無関係で、
  公開もされません
- `.claude/skills/` にはブログ運営用のスキルも入っています。アプリの作業とは別物です
